# ── API URL ───────────────────────────────────────────────────
# Local development: point to the local FastAPI server
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# ── Production (Vercel → Railway) ─────────────────────────────
# In the Vercel dashboard set these env vars for Production:
#
#   NEXT_PUBLIC_API_URL  = https://lawmateprod-production.up.railway.app
#   FASTAPI_INTERNAL_URL = https://lawmateprod-production.up.railway.app
#
# NEXT_PUBLIC_API_URL  — used by the browser for direct API calls (auth, etc.)
# FASTAPI_INTERNAL_URL — used server-side by Next.js API routes (agent proxy)
# NEVER set either of these to a Vercel URL; they must point to the Railway backend.
